import { CommaFormatPipe } from './comma-format.pipe';

describe('CommaFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CommaFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
